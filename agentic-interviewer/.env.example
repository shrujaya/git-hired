# Copy to .env.local and edit if your backend is not on the default port.
#   cp .env.example .env.local
#
# Must match HOST/PORT in server/.env. The WebSocket URL is derived from this
# automatically, so this is the only value to change.
VITE_API_BASE_URL=http://localhost:8100
